services:
  events:
    build: .
    ports:
      - "8080:8080"
    environment:
      BASE_URL: http://localhost:8080
      SECURE_COOKIES: "false"
    volumes:
      - events-data:/data
    restart: unless-stopped

volumes:
  events-data:
